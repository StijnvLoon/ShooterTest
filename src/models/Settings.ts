export class Settings {
    public static mousePosition: { y: number, x: number } = { y: 0, x: 0 }
    public static mouseDown: boolean = false
    public static mapSize: { width: number, height: number } = { width: 0, height: 0 }
}